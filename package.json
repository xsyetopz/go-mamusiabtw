{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "mamusiabtw-web-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.12",
  "scripts": {
    "build:web": "bun run --filter '*' build"
  }
}
