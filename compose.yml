services:
  imotherbtw:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SQLITE_PATH: /data/imotherbtw.sqlite
      MIGRATIONS_DIR: /app/migrations/sqlite
      LOCALES_DIR: /app/locales
      PLUGINS_DIR: /app/plugins
      IMOTHERBTW_PERMISSIONS_FILE: /app/config/permissions.json
    volumes:
      - ./data:/data
      - ./plugins:/app/plugins
      - ./config:/app/config:ro
